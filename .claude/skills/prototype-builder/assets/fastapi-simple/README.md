# API Server

A simple FastAPI backend service.

## Quick Start

```bash
make start
```

Server runs on http://localhost:8000

## Endpoints

- `GET /` - Hello World
- `GET /health` - Health check
- `GET /docs` - API documentation (Swagger UI)

## Development

Add new endpoints in `main.py`.
